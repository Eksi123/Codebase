1. "DFA.py"为有限自动机以及输出函数的源程序，"字符串匹配.py"为执行主程序。用户只需要在主程序文件操作即可。

2. 具体操作很简单，只需要在"字符串匹配.py"文件第4行按要求输入测试文件名即可，即输入"file_name"至Str_Find()中。（注意需包含英文引号，以及文件后缀名）

3. 要求：测试文件最好和程序文件在同一路径下，否则需输入"path_name/file_name"。另外，测试文件中除最后一行外，不允许中间出现空行。